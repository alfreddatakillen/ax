#!/bin/bash
if [ "$(whoami)" != "root" ]; then
	echo "must be root." >&2
	exit 1
fi

if [ ! -e /etc/machine-id ]; then
	cat /sys/class/dmi/id/product_uuid | sha512sum | sed 's/^\(.\{32\}\).*$/\1/' >/etc/machine-id
fi

cat /etc/machine-id
